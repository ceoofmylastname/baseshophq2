# Browser Vite client — publishable / anon key ONLY
# Service role NEVER lives in browser code. Edge functions hold service role
# internally via Supabase's SUPABASE_SERVICE_ROLE_KEY env var (auto-injected
# at function runtime; not set here).

VITE_SUPABASE_URL=https://oarstmxbgdczytwzpyxj.supabase.co
VITE_SUPABASE_ANON_KEY=sb_publishable_Hw3CJcDJR3bl-mJtP4a8Wg_cnt-3jeM

# Optional override for the Edge Functions base URL (defaults to
# `${VITE_SUPABASE_URL}/functions/v1` if unset).
# VITE_SUPABASE_FUNCTIONS_URL=https://oarstmxbgdczytwzpyxj.functions.supabase.co
